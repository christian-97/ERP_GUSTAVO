{
  "name": "sazon-api",
  "version": "1.0.0",
  "description": "API REST de Sazón con Cloudflare Workers y D1",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute sazon-db --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.109.2"
  }
}
